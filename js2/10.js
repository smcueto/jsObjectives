//After each step, log the value or question / answer.

//Create a const named cats that is an array with the following strings: Siamese, Calico, and Maine Coon
const cats = ['Siamese', 'Calico', 'Maine Coon'];
console.log(cats);
//Using indexes, display the first element of the cats array.
console.log(cats[0]);
//Create a const named aggregate that contains the same values as #9
const alpha = [];
const beta = [3, 4, 10];
const delta = ['hey', 'there']

const aggregate = [alpha, beta, delta];

console.log(aggregate);
//Using indexes on the aggregate array, display the value of the second integer in beta
console.log(aggregate[1][1]);