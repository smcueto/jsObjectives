//After each step, log the value or question / answer.

//Declare consts cats and aggregate with the values from #10
const alpha = [];
const beta = [3, 4, 10];
const delta = ['hey', 'there']

const aggregate = [alpha, beta, delta];

const cats = ['Siamese', 'Calico', 'Maine Coon'];
//Change the second value(Calico) in cats to Ragdoll


//Using indexes, add a fourth value Burmese to cats

//Using indexes, remove the first value from cats

//Using indexes on the aggregate array, change the value of the second integer in beta to 37
