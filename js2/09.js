//After each step, log the value or question / answer.

//Create three consts containing arrays: alpha which is empty, beta which contains three integers of your choice,
// and delta which contains two strings of your choice.
const alpha = [];
const beta = [3, 4, 10];
const delta = ['hey', 'there']

//Create a const named aggregate which is an array containing alpha, beta and delta
const aggregate = [alpha, beta, delta];
